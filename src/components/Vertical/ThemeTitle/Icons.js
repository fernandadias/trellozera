import {
  FiDatabase as data,
  FiMusic as music,
  FiCodepen as front,
  FiCoffee as back,
  FiFeather as design,
  FiFileText as product,
} from 'react-icons/fi';

const Icons = {
  data,
  music,
  front,
  back,
  design,
  product,
};

export default Icons;
