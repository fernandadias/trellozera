import styled from 'styled-components';

export const FooterText = styled.p`
  text-align: center;
  color: var(--footer);
  position: fixed;
  bottom: 16px;
  width: 100%;
  font-size: 11px;

  a{
    color: var(--themeColor);
    text-decoration: none;
  }

  svg{
    color: var(--themeColor);
    margin-bottom: -2px;
  }
`;

export const FootersText = styled.p``;
